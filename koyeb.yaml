name: veronica-pair
services:
  - name: app
    ports:
      - port: 8000
        http_path: /
    instances:
      type: nano
      scaling:
        min: 1
        max: 1
    env:
      - name: NODE_ENV
        value: production
      - name: PORT
        value: "8000"
